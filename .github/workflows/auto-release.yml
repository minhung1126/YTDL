name: "Auto Release"

on:
  # Trigger on tag push
  push:
    tags:
      - "v*"
  # Allow manual trigger
  workflow_dispatch:

jobs:
  auto-release:
    name: "Auto Release"
    runs-on: "ubuntu-latest"

    permissions:
      contents: write

    steps:
      - name: "Checkout code"
        uses: actions/checkout@v4

      - name: "Extract version info"
        id: version_extractor
        shell: bash
        run: |
          VERSION=$(grep '^__version__' YTDL.py | cut -d '"' -f 2)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
      - name: "Package Source Code"
        run: |
          zip "YTDL.${{ steps.version_extractor.outputs.version }}.zip" YTDL.py YTDL_mul.py

      - name: "Prepare release notes"
        run: |
          echo '點開 `Assets` 並下載 `YTDL.${{ steps.version_extractor.outputs.version }}.zip`（原始碼）。' > release_notes.md

      - name: "Create Release"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          gh release create ${{ steps.version_extractor.outputs.version }} \
            "YTDL.${{ steps.version_extractor.outputs.version }}.zip" \
            --title "Release ${{ steps.version_extractor.outputs.version }}" \
            --notes-file "release_notes.md" \
            --generate-notes
